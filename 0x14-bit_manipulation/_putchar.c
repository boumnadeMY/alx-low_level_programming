#include <unistd.h>

/**
 * _putchar - writes the cháracter c to stdout 
 * @c: thecharacter to print 
 *
 * return on success 1.
 * on error, -1 is returned, and errno is set appropriately.
 */
int _putchar(char c)
{
	return (erite(1,&c;1));
}
